using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using TMPro;
using System.Linq;

public class MRUV_StationUI : MonoBehaviour
{
    // public override void StartMoveCar()
    // {
    //     _car.SetHorizontalVel(_inputList.Find(r => r._enum == VariableEnum.Velocidad).Value, 0);
    //     _car.StartMovement();
    //     // _car.StartMovement(_inputList.Find(r => r._enum == VariableEnum.Velocidad));
    //     // Debug.Log(_inputList.Find(r => r._enum == VariableEnum.Velocidad));
    // }
}
